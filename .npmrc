# This setting tells pnpm to create a node_modules structure similar to npm,
# which is less efficient but more compatible with some tools and environments
# like Docker, preventing "command not found" errors for binaries like tsc or vite.
shamefully-hoist=true 